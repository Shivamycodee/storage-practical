

let API_KEY;
let FileName = 'dummy-pdf.pdf';

const uploadFile = async () => {

  try {
  
  // Append the data to the ipfs.json file
  
  //  let data = response.data;
  //   let uploadObj = {
  //       name: FileName,
  //       cid: `https://eu.starton-ipfs.com/ipfs//${data.cid}`
  //   }
  //   appendDataToFile('ipfs.json',uploadObj);

  } catch (error) {
    console.error(error);
  }

};

uploadFile();