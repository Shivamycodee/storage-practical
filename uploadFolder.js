

let API_KEY;
let FolderPath = 'dummy-folder';

const uploadFolder = async () => {

  try {

  // Append the data to the ipfs.json file

//   let responseData = response.data;
//     let uploadObj = {
//         name: FolderPath,
//         cid: `https://eu.starton-ipfs.com/ipfs//${responseData.cid}`
//     }
//     appendDataToFile('ipfs.json',uploadObj);

  } catch (error) {
    console.error(error);
  }

};

uploadFolder();